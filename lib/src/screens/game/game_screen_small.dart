import 'package:flutter/material.dart';

class GameScreenSmall extends StatelessWidget {
  const GameScreenSmall({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}